# ⚠️ このフォルダは独立git管理下ですが、U-WAN本社マッチョ運用体制の配下です

**このプロジェクトフォルダは、デプロイ・バージョン管理の都合で独立した`.git`を持っていますが、組織上は`U-WAN本社`の一部です。**

## 最優先ルール

作業を始める前に、必ず `../../../CLAUDE.md`（U-WAN本社 運用指示書本体）を読み込むこと。

- 棚原さんへの依頼受領・進捗報告・完了報告は、必ずマッチョ（COO型AI）を経由する
- このフォルダのセッションが、特定のAI社員（ハル・ケン・ルイ等）や無名の技術アシスタントとして棚原さんに直接応答してはならない
- 棚原さんが「マッチョ来て」と言った場合、または棚原さんがこのフォルダで直接セッションを開始した場合も、まず `../../../CLAUDE.md` を読み込んでからU-WAN運用ルールに従う
- 唯一の例外：Google/Vercel等の外部サービスがブラウザ認証・権限承認を棚原さん本人にしか求められない場合のみ、その操作依頼を直接行ってよい。ただし作業内容の報告・完了連絡は改めてマッチョ経由で行う

詳細は `../../../CLAUDE.md` を参照。

---

## 最優先ルール：承認待ちの変更を `main` に積まない（2026-09-12 棚原承認）

**このリポジトリの `main` は「棚原承認済み＝いつ本番に出てもよいもの」だけを置く場所とする。承認待ちの作業は必ず作業ブランチに積む。**

### 具体ルール

1. **新しい変更は作業ブランチを切ってからコミットする**（例：`work/ai-seminar-oto`・`work/<案件名>`）。`main` に直接コミットしてよいのは、棚原承認が既に出ているものだけ。
2. **棚原承認が出たら**、作業ブランチを `main` にマージし、`git push origin main` する。承認前のpushは従来どおり禁止。
3. **承認前に「commitだけしてpushは我慢する」は成立しないと考える。** このリポジトリは他セッションの自動ジョブ（marathonダッシュボードの日次更新等）が `git push origin main` を打つため、`main` に積んだ時点で第三者のpushに巻き込まれて本番に出る。
4. 作業ブランチが不要になったら削除せず放置してよいが、古いブランチを作業再開の土台に使わないこと（`deploy-ai-seminar` は2026-09-05以前の残骸で、mainより6コミット古い）。

### 理由

2026-09-11、OTOページ（`ai-seminar-oto/`）を棚原承認前の状態で `main` にコミットして保留していたところ、同日23:38に別セッションのmarathonダッシュボード自動更新が `git push origin main` を実行し、**承認待ちのOTOページ2コミットが一緒に本番公開された**。GitHub Pagesのデプロイまで完走していた。

原因は個々のAI社員の不注意ではなく、**「pushしなければ公開されない」という前提がこのリポジトリでは成り立たないこと**。`main` は複数セッションの共有レーンであり、そこに承認待ちのものを置けば、いつか誰かのpushで出る。承認ゲートはpushのタイミングではなくブランチの分離で担保する。

### 適用

- このリポジトリ（`00_CompanyOS/98. HP/site`）で作業する全AI社員（ルイ・ケン・ゲン・ハル等）とマッチョ
- マッチョがこのリポジトリの作業を振るときは、ブリーフに「作業ブランチを切ってコミットすること」を明記する
- 同じ構造（自動ジョブがpushする共有リポジトリ）を持つ他のサブプロジェクトにも、気づいた時点で同ルールを提案する
