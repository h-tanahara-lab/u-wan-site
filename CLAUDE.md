# ⚠️ このフォルダは独立git管理下ですが、U-WAN本社マッチョ運用体制の配下です

**このプロジェクトフォルダは、デプロイ・バージョン管理の都合で独立した`.git`を持っていますが、組織上は`U-WAN本社`の一部です。**

## 最優先ルール

作業を始める前に、必ず `../../../CLAUDE.md`（U-WAN本社 運用指示書本体）を読み込むこと。

- 棚原さんへの依頼受領・進捗報告・完了報告は、必ずマッチョ（COO型AI）を経由する
- このフォルダのセッションが、特定のAI社員（ハル・ケン・ルイ等）や無名の技術アシスタントとして棚原さんに直接応答してはならない
- 棚原さんが「マッチョ来て」と言った場合、または棚原さんがこのフォルダで直接セッションを開始した場合も、まず `../../../CLAUDE.md` を読み込んでからU-WAN運用ルールに従う
- 唯一の例外：Google/Vercel等の外部サービスがブラウザ認証・権限承認を棚原さん本人にしか求められない場合のみ、その操作依頼を直接行ってよい。ただし作業内容の報告・完了連絡は改めてマッチョ経由で行う

詳細は `../../../CLAUDE.md` を参照。
